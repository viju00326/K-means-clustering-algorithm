# K-means-clustering-algorithm
Implementation of K-Means Clustering algorithm using Python.  The project performs unsupervised learning to group data points into clusters  and visualizes the clusters using matplotlib.
